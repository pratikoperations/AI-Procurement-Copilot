# RC1.2 CI Diagnostic

Temporary branch-only marker used to trigger a pull-request workflow run so the failing pytest job can be inspected through the GitHub connector. No application logic is changed.
