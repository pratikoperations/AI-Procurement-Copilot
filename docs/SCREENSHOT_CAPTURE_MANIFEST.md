# AI Procurement Copilot v1.2 — Screenshot Capture Manifest

## Current status

The SVG application-view assets in `docs/assets/screens/` are illustrative portfolio visuals built from synthetic, generic content. They are not represented as direct hosted screenshots.

Direct hosted captures remain pending because no current v1.2 preview URL has been verified. The historical Recovery R1 URL must not be reused as current v1.2 evidence.

## Required genuine capture set

1. **Hero screenshot** — executive landing view with application identity, status pills and decision-support purpose.
2. **RFQ comparison** — category-aware supplier comparison with sanitized data.
3. **Validation control** — validation status and award-language withholding.
4. **Executive outputs** — business-facing outputs and separated audit downloads.

## Required device widths

- 360 px
- 390 px
- 412 px
- tablet width
- standard desktop width

## Capture rules

- Use the final v1.2 preview branch and verified deployment URL.
- Use synthetic or sanitized content only.
- Ensure the Interview Guide is absent.
- Hide development controls where possible.
- Do not crop validation status, navigation labels or limitations.
- Do not show confidential filenames, supplier names or commercial values.
- Do not imply production deployment, live ERP integration or realized savings.

## Acceptance checks

- No clipped navigation label.
- No horizontal page scrolling for normal navigation.
- Headings and status labels remain readable.
- KPI cards stack without overflow.
- Tables remain locally scrollable where needed.
- Download controls remain reachable.
- Mobile and desktop captures match the final reviewed head.

## Replacement rule

Once genuine captures are approved, they may replace the illustrative SVG application-view assets or be added as PNG evidence. The illustrative label must not be removed until the direct hosted origin is verified.