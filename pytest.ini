[pytest]
pythonpath = .
testpaths = tests
addopts = --collect-only -q -k "download and app and buttons and (expose or contain or keep or show or preserve or remain or visible or expected or exactly)"
