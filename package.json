{
  "name": "ai-procurement-copilot-mobile-acceptance",
  "private": true,
  "version": "1.0.0",
  "description": "Governed Playwright acceptance harness for mobile and foldable browser readiness.",
  "scripts": {
    "test:mobile": "playwright test --config=playwright.config.ts",
    "test:mobile:update": "playwright test --config=playwright.config.ts --update-snapshots",
    "audit:mobile": "npm audit --audit-level=high"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
